<h1><%= t("update_coin")%></h1>

<%= render 'form', coin: @coin %>
<div>
<%= link_to t('links.show'), @coin, class:"btn btn-primary" %> 
<%= link_to t('links.back'), coins_path, class:"btn btn-danger" %>
</div>